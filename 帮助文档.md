# 短信自动回复 · 使用帮助

## 目录

1. [应用简介](#1-应用简介)
2. [首页概览](#2-首页概览)
3. [规则管理](#3-规则管理)
   - 3.1 [新建规则](#31-新建规则)
   - 3.2 [编辑规则](#32-编辑规则)
   - 3.3 [匹配条件详解](#33-匹配条件详解)
   - 3.4 [执行动作详解](#34-执行动作详解)
   - 3.5 [启用/禁用规则](#35-启用禁用规则)
   - 3.6 [删除规则](#36-删除规则)
4. [设置](#4-设置)
   - 4.1 [开关服务](#41-开关服务)
   - 4.2 [白名单模式](#42-白名单模式)
   - 4.3 [黑名单](#43-黑名单)
5. [操作日志](#5-操作日志)
6. [钉钉群机器人配置](#6-钉钉群机器人配置)
7. [常见问题](#7-常见问题)

---

## 1. 应用简介

**短信自动回复** 是一款 Android 工具应用，能够自动监控收到的短信，根据您预设的规则执行以下操作：
- ✅ **自动回复** — 自动回复一条短信给对方
- ✅ **短信转发** — 把收到的短信通过短信转发到另一个号码
- ✅ **钉钉推送** — 把收到的短信推送到钉钉群机器人（免费，不花短信费）
- ✅ **组合动作** — 回复+转发、回复+钉钉推送

适用于：备用机短信管理、验证码自动转发、工作号码自动回复等场景。

---

## 2. 首页概览

> 📸 *截图：首页界面*

打开应用后进入主界面，三个主要入口：

| 按钮 | 功能 |
|------|------|
| 📋 规则管理 | 管理所有自动规则（新建、编辑、启用、删除） |
| 📄 操作日志 | 查看短信处理历史记录 |
| ⚙️ 设置 | 服务开关、白名单/黑名单设置 |

应用会在后台自动运行，无需手动开启。

---

## 3. 规则管理

> 📸 *截图：规则列表页面*

规则是本应用的核心。每条规则包含：
- **规则名称** — 方便记忆的标识
- **匹配条件** — 满足什么条件才触发（关键词 + 发送者号码，两者**同时满足**才触发）
- **执行动作** — 触发后做什么（回复短信、转发、钉钉推送等）

### 3.1 新建规则

1. 点击首页的"规则管理"
2. 点击右下角的 **+** 按钮
3. 填写规则信息后保存

### 3.2 编辑规则

> 📸 *截图：规则编辑页面（含标注）*

在规则列表中点击任意规则，即可进入编辑界面修改。

### 3.3 匹配条件详解

每条规则有**两个匹配条件**，两者是**且**的关系（必须同时满足才触发）：

**① 关键词匹配**

| 模式 | 说明 | 示例 |
|------|------|------|
| 包含 | 短信内容包含关键词 | 关键词"验证码"，短信"您的验证码是1234" → 匹配 |
| 完全匹配 | 短信内容和关键词完全相同 | 关键词"OK"，短信"OK" → 匹配 |
| 开头匹配 | 短信以关键词开头 | 关键词"【银行】"，短信"【银行】您的账户…" → 匹配 |
| 正则表达式 | 用正则匹配（高级） | 正则`\d{6}`，短信含6位数字 → 匹配 |

关键词留空 = 不限制，所有短信都匹配。

**② 发送者号码匹配**

| 模式 | 说明 | 示例 |
|------|------|------|
| 精确匹配 | 号码完全相同 | 完全匹配 13800138000 |
| 开头匹配 | 号码以指定前缀开头 | 前缀"106"，营销号 1069xxxxx → 匹配 |
| 包含 | 号码包含指定内容 | 含"400"，400电话 → 匹配 |
| 任意号码 | 不限制号码 | 所有号码都匹配 |

号码留空 = 不限制，所有号码都匹配。

### 3.4 执行动作详解

> 📸 *截图：动作选择下拉框，展示不同动作时下方输入框动态变化*

| 动作 | 需要填写 | 说明 |
|------|----------|------|
| 回复短信 | 回复内容 | 自动回复一条短信给发件人 |
| 短信转发 | 手机号码 | 把短信通过短信转发到另一个手机号（会花短信费） |
| 钉钉推送 | Webhook 地址 | 把短信推送到钉钉群（免费，需要网络） |
| 回复+短信转发 | 回复内容 + 手机号 | 既回复又转发 |
| 回复+钉钉推送 | 回复内容 + Webhook | 回复短信同时推送到钉钉群 |

**关于转发目标**：
- 选择"短信转发"时，填写**手机号码**（纯数字，如 13800138000）
- 选择"钉钉推送"时，填写**钉钉群机器人 Webhook 地址**（以 https 开头）
- 应用会自动根据内容的格式判断走短信还是钉钉

### 3.5 启用/禁用规则

> 📸 *截图：规则列表中的开关*

在规则列表中，每条规则右侧有个开关按钮：
- **绿色开启** — 该规则生效，收到匹配短信时自动执行
- **灰色关闭** — 该规则暂时失效

### 3.6 删除规则

点击规则列表右侧的垃圾桶图标即可删除（无二次确认，请谨慎操作）。

---

## 4. 设置

> 📸 *截图：设置页面*

### 4.1 开关服务

**自动回复/转发服务**：控制整个应用是否工作。
- 开启：后台监听短信，按规则自动处理
- 关闭：应用不处理任何短信

### 4.2 白名单模式

> 📸 *截图：白名单开关+号码输入*

开启后，**只有白名单中指定的号码**发来的短信才会被处理（其他号码都被忽略）。
- 先打开"白名单模式"开关
- 在下方的输入框中填写允许的号码
- 多个号码用**逗号（英文逗号）**分隔，如：`13800138000,13900139000`
- 也支持前缀匹配：填 `106` 会自动匹配所有以 106 开头的号码

不开启白名单模式时，应用会处理所有号码的短信（除非在黑名单中）。

### 4.3 黑名单

> 📸 *截图：黑名单输入区域*

**黑名单始终有效**，无论白名单是否开启。

填入的号码永远不会被处理。例如：
- 把一些营销号加入黑名单
- 同时开启白名单模式时，白名单内的号码会被处理，但如果在白名单和黑名单中同时出现，该号码会被忽略

---

## 5. 操作日志

> 📸 *截图：日志列表*

"操作日志"记录了应用处理短信的每一次操作，包括：
- 收到短信
- 执行了回复/转发/钉钉推送
- 没有匹配到规则
- 号码被过滤

日志按时间倒序排列，最新的在前面。可点击菜单中的"清空日志"清除所有记录。

---

## 6. 钉钉群机器人配置

想要接收短信推送，需要先在钉钉中创建一个群机器人：

### 步骤

1. **打开钉钉**，进入一个群聊（或新建一个群）
2. 点击群设置 → **智能群助手** → **添加机器人**
3. 选择 **自定义（通过Webhook接入）**
4. 设置机器人名称（如"短信通知"），点击完成
5. 复制生成的 **Webhook 地址**（以 `https://oapi.dingtalk.com/robot/send?access_token=` 开头）
6. **安全设置**：建议选择"自定义关键词"，填入一个关键词如"短信"（这样只有包含该关键词的消息才会推送，是免费额度管理的好方法）
7. 在应用内编辑规则时，将 Webhook 地址粘贴到"转发目标"输入框

> ⚠️ 钉钉群机器人免费、无数量限制，但每个机器人每分钟最多 20 条消息。如果短信量大，建议建多个群、多个机器人分摊。

---

## 7. 常见问题

### Q1：应用会不会拦截正常短信？
不会。应用**只读取**收到的短信，不会阻止其他应用（如系统短信）收到短信。它类似于"在旁边看一眼"然后决定是否回复/转发。

### Q2：开启服务后耗电吗？
极小。应用只在收到短信时才执行处理逻辑，平时几乎不消耗资源。

### Q3：如果手机没有网络，钉钉推送还能用吗？
不能。钉钉推送需要网络连接。没有网络时，如果规则也配置了"回复"，回复短信不受影响（短信不依赖网络）。

### Q4：多条规则同时匹配怎么办？
应用会从上到下执行**第一个匹配**的规则。匹配到一条后就停止，不会重复执行多条。

### Q5：转发短信会产生费用吗？
- **短信转发**：会消耗您 SIM 卡的短信套餐（按照普通短信计费）
- **钉钉推送**：通过互联网发送，免费
- **自动回复**：会消耗短信套餐（回复一条短信给发件人）

### Q6：应用被后台杀死怎么办？
某些手机（尤其是 MIUI/EMUI/ColorOS 等）会限制后台运行。建议：
- 将应用加入**自启动白名单**
- 锁定应用在**最近任务**中（下拉锁定）
- 关闭系统的**省电优化**对该应用的限制

### Q7：如何导出/备份规则？
当前版本暂不支持导出。建议截图保存每个规则的配置内容。

---

## 版本信息

当前版本：v19（v16-v19 为界面美化版本）
开发者：-
最后更新：2026-05-22
